version: "3.8"

services:
  pmm-server:
    volumes:
      - ${PMM_VOLUME_NAME}:/srv
  watchtower:
    volumes:
      - ${WATCHTOWER_VOLUME_NAME}:/var/run/docker.sock
volumes:
  pmm-prod-data:
    external: true
  watchtower-prod-data:
    external: true
